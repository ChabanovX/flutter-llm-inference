# Run with `dart run ffigen --config ffigen.yaml`.
name: LlamaInferenceBindings
description: |
  Bindings for `src/llama_inference.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/llama_inference_bindings_generated.dart'
headers:
  entry-points:
    - 'src/llama_inference.h'
  include-directives:
    - 'src/llama_inference.h'
ffi-native:
comments:
  style: any
  length: full
